return {
  "nvim-telescope/telescope.nvim",
  -- the readme of telescope is incorrect. see: https://github.com/nvim-telescope/telescope.nvim/issues/3487
  -- there is no need for using a release tag
  -- tag = "0.1.8",
  requires = { "nvim-lua/plenary.nvim" }
}
